# Procfile for Railway/Heroku deployment (not used with AWS EC2)
# For AWS EC2, run directly: uvicorn analyze_api:app --host 0.0.0.0 --port 8001 --workers 4
web: uvicorn analyze_api:app --host 0.0.0.0 --port $PORT
